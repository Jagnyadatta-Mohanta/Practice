const score = 400;
console.log(score);

const balance = new Number(100)
// there are various functions for numbers given in JS read them in detail from MDN.JS

console.log(balance);
// it will give you an array type output.

// math library comes by default with JS

// math.abs() - changes negative numbers to positive but not vice versa.

// math.min() and math.max() gives minimum and maximum numbers from a given set.

// math.random() this functioon generates a random number between 0 to 1 generally, but we can multiply  10 or more to get a random number as per our requirment and to avoid 0 e can further add a number to it.
//EX-
console.log(Math.random());// it will generate any random number between 0 to 1.
console.log(Math.random()*10);// it will give a ______.
console.log((Math.random()*10) + 1);// it will avoid 0.




